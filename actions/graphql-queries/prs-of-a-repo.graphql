query ($owner: String!, $name: String!) {
  repository(owner: $owner, name: $name) {

    openPRs:pullRequests(last:100, states:[MERGED, OPEN]) {
      nodes {
        author {
          login
          
        }
        number
        state
      }
    }
  }
}